function startLevel(gameId) {
    console.log("Starting game: " + gameId);
    // In the future, this will redirect to your game folders
    // window.location.href = `games/${gameId}/game.html`;
    alert("Prepare for battle! Loading: " + gameId);
}